import { Button, Tab, TabList, Tabs, TabPanels, TabPanel, Box, Heading, Text, Breadcrumb, BreadcrumbItem, BreadcrumbLink, Link, Flex, Center } from "@chakra-ui/react";

export default function Test() {
    return (
        <>
        <Center pt={'1.5rem'} >
            we
        </Center>
            <Flex alignItems={'center'} justifyContent={'center'}>

            wewe
            </Flex>
        </>
    )
}